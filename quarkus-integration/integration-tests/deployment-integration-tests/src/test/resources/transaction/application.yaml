quarkus:
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:transaction-contract;DB_CLOSE_DELAY=-1
vanillabp:
  adapters:
    demo1:
      type: dummy
  workflow-modules:
    test-module:
      adapters:
        demo1:
          resources-location: classpath:processes/dummy
