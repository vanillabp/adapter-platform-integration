quarkus:
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:portable-values-declared;DB_CLOSE_DELAY=-1
vanillabp:
  adapters:
    demo1:
      type: dummy
      test: 1
  workflow-modules:
    test-module:
      adapters:
        demo1:
          resources-location: classpath:processes/dummy
      workflows:
        LoanApprovalProcess:
          declared-aggregate-values:
            - amount
          tasks:
            assessRisk:
              declared-task-params:
                - riskReport
