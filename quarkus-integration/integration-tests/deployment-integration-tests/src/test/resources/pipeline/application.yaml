quarkus:
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:pipeline;DB_CLOSE_DELAY=-1
vanillabp:
  adapters:
    demo:
      type: dummy
  workflow-modules:
    test-module:
      adapters:
        demo:
          resources-location: classpath:processes/dummy
