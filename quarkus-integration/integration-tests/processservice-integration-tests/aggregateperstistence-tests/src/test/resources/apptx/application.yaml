vanillabp:
  adapters:
    test:
      type: dummy
      test: 1
  workflow-modules:
    test-module:
      adapters:
        test:
          resources-location: classpath:processes/dummy
      workflows:
        TestProcess:
          allow-full-sync-with-bpms: true
          declared-aggregate-values:
            - '*'
